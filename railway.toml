[deploy]
startCommand = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"

[[services]]
name = "token-refresh"
startCommand = "python -m app.jobs.refresh_tokens"

[services.cron]
schedule = "0 * * * *"
